import { ModStringPipe } from './mod-string.pipe';

describe('ModStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ModStringPipe();
    expect(pipe).toBeTruthy();
  });
});
